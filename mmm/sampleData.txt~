from mmm.mmm_app import *
from django.contrib.auth.models import User
from django.utils import timezone

# User0
u = User.objects.create_user(
	'user0', 
	'user0@fakemail.com', 
	'user0_pswd',
	first_name='User', 
	last_name='Zero', 
)
u.save()

ui = UserInfo(
	user = u,
	is_sponsor=False, 
	is_developer=True,
	setting_0=True,
	setting_1=True,
	setting_2=True,
)
ui.save()

